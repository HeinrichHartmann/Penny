{
  "name": "penny-frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1 --port 8000 --strictPort --open",
    "test:ui": "playwright test --config=tests/ui/playwright.config.mjs"
  },
  "dependencies": {
    "echarts": "^5.6.0",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "vite": "^7.1.9"
  }
}
